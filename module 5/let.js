/*if(true){
    let c= 40
    console.log(c); // 40
}
console.log(c); // ReferenceError: c is not defined
// last console.log(c) will throw an error because 'let' is block scoped.
*/

// we can reassign a variable declared with 'let'
// let c =90;
// c=33
// console.log(c); // 33

//  within its scope, the mean of the within its scope is that we can reassign the variable within the same block or function where it was declared, but not outside of it.
// for example:
/*
if(true){
    let c = 40;
    console.log(c); // 40
}
console.log(c); // ReferenceError: c is not defined. */


